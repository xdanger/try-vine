// Factorial example in Vine

use std::IO;

fn factorial(n) {
  if n <= 1 {
    return 1
  } else {
    return n * factorial(n - 1)
  }
}

pub fn main(&io: &IO) {
  let n = 5
  io.println("Factorial of {n} is {factorial(n)}")
}