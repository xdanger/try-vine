// Factorial example in Vine

fn factorial(n: Int) -> Int {
  if n <= 1 {
    return 1
  } else {
    return n * factorial(n - 1)
  }
}

fn main() {
  let n = 5
  println("Factorial of " + n.toString() + " is " + factorial(n).toString())
}