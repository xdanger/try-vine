// Factorial example in Vine

use std::IO;

// 使用迭代的方式计算阶乘
pub fn main(&io: &IO) {
  let n = 5
  let result = 1

  // 计算 n!
  let i = 1
  while i <= n {
    result = result * i
    i = i + 1
  }

  io.println("Factorial of {n} is {result}")
}